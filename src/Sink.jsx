// CodeSandbox.js

function Sink(props) {
    return (
      <div>
        <h1> {props.sink} </h1>
      </div>
    );
  }
  
  // Must export the component's function (or class)
  export default Sink;